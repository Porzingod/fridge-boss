const initialState = {
  cuisineFilter: null,
  allergyFilter: null,
}

export const filtersReducer = (state = initialState, {type, payload}) => {
  switch(type) {
    default:
      return state
  }
}
